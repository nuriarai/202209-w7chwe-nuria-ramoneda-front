import styled from "styled-components";

const LoginFormStyled = styled.form`
  &.login {
    max-width: 400px;
    margin: 0 auto;
  }
  label {
    display: block;
    padding-bottom: 5px;
  }
  .login__wrap-input {
    margin-bottom: 15px;
  }
`;

export default LoginFormStyled;
